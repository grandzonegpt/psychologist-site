node_modules/
.env
.env.local

# Persistent state (Railway volume contents). When BOOKING_DATA_DIR is
# unset, these files land next to bot.js / server.js and must not be
# committed.
chat.json
schedule.json
processed-events.json
data/

# Reports + scratch outputs
reports/
*.json.bak

# Local-only scripts (can stay tracked, but their dated outputs do not)
scripts/gsc-deep-report-*.json
scripts/gsc-indexation-detailed-*.json
